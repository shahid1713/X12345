import { Matcher } from './Matcher';
export declare class AnyListMatcher<T> extends Matcher<Array<T>> {
    private readonly matchers;
    private readonly sliceMatchers;
    constructor(matchers: Array<Matcher<T>>);
    matchValue(array: unknown, keys: ReadonlyArray<PropertyKey>): array is Array<T>;
}
export declare function anyList<T>(...elements: Array<Matcher<T>>): Matcher<Array<T>>;
