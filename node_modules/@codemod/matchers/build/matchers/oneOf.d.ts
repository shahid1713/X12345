import { Matcher } from './Matcher';
export declare class OneOfMatcher<T> extends Matcher<[T]> {
    private readonly matcher;
    constructor(matcher: Matcher<T>);
    matchValue(value: unknown, keys: ReadonlyArray<PropertyKey>): value is [T];
}
export declare function oneOf<T>(matcher: Matcher<T>): Matcher<[T]>;
