import { Matcher } from './Matcher';
export declare class ArrayOfMatcher<T> extends Matcher<Array<T>> {
    private readonly elementMatcher;
    constructor(elementMatcher: Matcher<T>);
    matchValue(value: unknown, keys: ReadonlyArray<PropertyKey>): value is Array<T>;
}
export declare function arrayOf<T>(elementMatcher: Matcher<T>): Matcher<Array<T>>;
