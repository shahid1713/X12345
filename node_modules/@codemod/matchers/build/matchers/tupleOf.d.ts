import { Matcher } from './Matcher';
export declare class TupleOfMatcher<T, A extends Array<T> = Array<T>> extends Matcher<A> {
    private readonly matchers;
    constructor(...matchers: Array<Matcher<T>>);
    matchValue(value: unknown, keys: ReadonlyArray<PropertyKey>): value is A;
}
export declare function tupleOf<T, A extends Array<T> = Array<T>>(...matchers: Array<Matcher<T>>): Matcher<A>;
