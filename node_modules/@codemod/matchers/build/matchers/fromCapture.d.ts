import { CapturedMatcher } from './capture';
import { Matcher } from './Matcher';
export declare class FromCaptureMatcher<T> extends Matcher<T> {
    private readonly capturedMatcher;
    constructor(capturedMatcher: CapturedMatcher<T>);
    matchValue(value: unknown): value is T;
}
export declare function fromCapture<T>(capturedMatcher: CapturedMatcher<T>): Matcher<T>;
